---
id: "q-gen-sql-select-2-09"
exam: ""
number: 09
type: "single"
generated: true
tags: ["AI-generated"]
correct: ["b"]
pdfs: ["sql-select-2"]
---

According to the presentation, what benefit does using GROUP BY (returning summaries instead of detailed records) provide?

- a) It guarantees that queries always run faster than any index
- b) It reduces resource use on both the server side and the client side
- c) It prevents NULL values from ever appearing
- d) It removes the need for a WHERE clause

### Explanation

The GROUP BY discussion slide states that GROUP BY allows reducing resource use both server-side and client-side whenever summary values, rather than detailed records, are required.
