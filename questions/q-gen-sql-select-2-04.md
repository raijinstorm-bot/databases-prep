---
id: "q-gen-sql-select-2-04"
exam: ""
number: 04
type: "single"
generated: true
tags: ["AI-generated"]
correct: ["c"]
pdfs: ["sql-select-2"]
---

The presentation notes that the argument of an aggregate function can be more than a bare column name. Which example is given?

- a) `SUM(*)`
- b) `COUNT(NetValue, Tax)`
- c) `SUM(NetValue*1.23)`
- d) `AVG()` with no argument

### Explanation

The aggregate functions slide notes an expression can be just a column name like `NetValue` or be based on a column value, giving `SUM(NetValue*1.23)` as the example. The other forms are not valid per the slide.
