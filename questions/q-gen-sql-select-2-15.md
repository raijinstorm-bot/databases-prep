---
id: "q-gen-sql-select-2-15"
exam: ""
number: 15
type: "multi"
generated: true
tags: ["AI-generated"]
correct: ["a", "b", "d"]
pdfs: ["sql-select-2"]
---

According to the slides on the R-based data analysis examples, which statements are accurate?

- a) Modern analysis can mix matrix-like and file-based operations with SQL
- b) The sample code is developed in R (using packages like `sqldf`)
- c) The SQL must be executed only against a remote production database
- d) SQL code can be executed on top of in-memory data imported from a text file

### Explanation

The analysis slides state the code is written in R, that modern analysis mixes matrix/file operations with SQL, and that SQL is executed on top of in-memory data imported from a text file. There is no requirement to run against a remote production database.
