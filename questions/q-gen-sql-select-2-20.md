---
id: "q-gen-sql-select-2-20"
exam: ""
number: 20
type: "single"
generated: true
tags: ["AI-generated"]
correct: ["c"]
pdfs: ["sql-select-2"]
---

According to the aggregate functions table, how do `MIN()` and `MAX()` determine the lowest/largest value of an expression?

- a) Always by converting values to text and comparing alphabetically
- b) By counting how many times each value occurs
- c) By comparing values based on their type, the same way they are compared for sorting
- d) By returning the first and last row physically stored

### Explanation

The aggregate functions table states that MIN and MAX compare values based on their type, in the same way they are compared to be sorted. They are not based on counts or physical row order.
